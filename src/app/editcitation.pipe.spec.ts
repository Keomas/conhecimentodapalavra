import { EditcitationPipe } from './editcitation.pipe';

describe('EditcitationPipe', () => {
  it('create an instance', () => {
    const pipe = new EditcitationPipe();
    expect(pipe).toBeTruthy();
  });
});
